<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" xintegrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('partials/navbar', {page: "/lego/sets"}) %>

    <div class="container" style="padding-top: 80px;">
        <div class="row">
            <main class="col-md-12">
                <h2>Collection</h2>
                <hr />

                <!-- Hard-coded links for filtering by theme -->
                <div class="mb-4">
                    Filter by Theme:
                    <a href="/lego/sets?theme=Technic" class="btn btn-outline-primary btn-sm me-2">Technic</a>
                    <a href="/lego/sets?theme=Classic Town" class="btn btn-outline-primary btn-sm me-2">Classic Town</a>
                    <a href="/lego/sets?theme=Space" class="btn btn-outline-primary btn-sm me-2">Space</a>
                    <a href="/lego/sets" class="btn btn-outline-secondary btn-sm">Show All</a>
                </div>

                <div class="table-responsive">
                    <table class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Image</th>
                                <th>Name</th>
                                <th>Theme</th>
                                <th>Year</th>
                                <th>Parts</th>
                                <th></th> <!-- For Details Button -->
                            </tr>
                        </thead>
                        <tbody>
                            <% if (sets.length > 0) { %>
                                <% sets.forEach(set => { %>
                                    <tr>
                                        <td>
                                            <div class="rounded" style="width: 8rem; height: 8rem; background-image: url('<%= set.img_url %>'); background-size: cover; background-position: center;"></div>
                                        </td>
                                        <td><%= set.name %></td>
                                        <td>
                                          <% if (set.theme) { %>
                                            <a href="/lego/sets?theme=<%= set.theme.name %>" class="btn btn-outline-info btn-sm"><%= set.theme.name %></a>
                                          <% } else { %>
                                            <span class="text-muted">No Theme</span>
                                          <% } %>
                                        </td>
                                        <td><%= set.year %></td>
                                        <td><%= set.num_parts %></td>
                                        <td><a href="/lego/sets/<%= set.set_num %>" class="btn btn-outline-dark btn-sm">Details</a></td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="6">No sets found.</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" xintegrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
